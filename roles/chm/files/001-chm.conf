# Default Apache virtualhost template

<VirtualHost *:80>
    ServerAdmin webmaster@localhost
    DocumentRoot /var/www
    ServerName ComeHireMe

    <Directory /var/www>
        AllowOverride All
        Require all granted
    </Directory>
</VirtualHost>

